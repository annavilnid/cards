import { Outlet } from "react-router-dom";
import { FormWrapper } from "./FormStyle";

export const Form = () => {
  return (
    <FormWrapper>
      <h1>Form</h1>
      {/*<Outlet />*/}
    </FormWrapper>
  );
};
